#include <stdio.h>
#include <stdlib.h>

double calculerDelta (double a, double b, double c, double racine[2]){
	racine[0]=
	racine[1]
}


int main(){
	double a,b,c, racine[2];
	
	printf ('entrez les valeurs des 3 premiers polynomes');
	scanf("%lf", a)
	scanf("%lf", b)
	scanf("%lf", c)
	resoudre(a,b,c,racine[2])
	
	return 0;
}


	/*double monReel;
		printf("entrez un réel \n");
		scanf("%lf",&monReel);
		printf("Mon réel avec tous les chiffres après la virgule  %lf \n",monReel);
		printf("Mon réel avec 2 chiffres après la virgule  %.2lf \n",monReel);
		printf("Mon réel avec 3 chiffres après la virgule  %.3lf \n",monReel);*/

